<% include ./partials/header.ejs %>

    <div class="container">
      <div class="jumbotron">
        <% if (title == "Home") { %>
            <% include ./contents/home.ejs %>
        <% } %>
        <% if (title == "About") { %>
         <% include ./contents/about.ejs %>
        <% } %>
        <% if (title == "Projects") { %>
         <% include ./contents/project.ejs %>
        <% } %>

        <% if (title == "Services") { %>
          <% include ./contents/service.ejs %>
            <!-- <a class="btn btn-primary" href="/contact-list" role="button">Contact List</a> -->
         <% } %>
        <% if (title == "Contact") { %>
         <% include ./contents/contact.ejs %>
           <!-- <a class="btn btn-primary" href="/contact-list" role="button">Contact List</a> -->
        <% } %>
      </div>
    </div>

<% include ./partials/footer.ejs %>
